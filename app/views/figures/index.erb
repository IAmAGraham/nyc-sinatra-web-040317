<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Historical Figure's Name</h1>

    <%@figures.each do |figure|%>
      <a href="/figures/<%= figure.id %>"><%= figure.name %></a>
    <ul>
      <%figure.titles.each do |title|%>
        <li><%=title.name%></li>
        <%end%>
    </ul>
    <% end %>
    
  </body>
</html>
