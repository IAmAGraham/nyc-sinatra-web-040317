<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <p>Histoical Figure's Name:</p>
    <p><a href="/figures/<%= @figure.id %>"><%= @figure.name %></a></p>

    <ul>Landmarks:
        <%@figure.landmarks.each do |landmark|%>
        <li><%=landmark.name%></li>
        <%end%>
      </ul>

    <ul>Titles:
        <%@figure.titles.each do |title|%>
        <li><%=title.name%></li>
        <%end%>
      </ul>

  </body>
</html>
